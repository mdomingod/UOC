# Scripts

1. Create the script
   script.py
2. Add the shebang at the top:
   #!/usr/bin/env python3
3. Make it executable. Run the command on the script directory.
   chmod +x generate_json.py
4. Run it directly (from the terminal):
   ./script.py